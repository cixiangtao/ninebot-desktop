# 隐私说明

骑迹是本地优先的只读九号数据客户端。目前不包含账号系统、遥测、广告、崩溃上报或自建云端服务。

## 本机保存的数据

- ninecli 登录令牌、ninecli 自身的车辆缓存和非敏感配置保存在 Electron `userData/ninecli` 目录。
- macOS 上配置目录权限设为 `0700`，已知配置与令牌文件权限设为 `0600`；Windows 上使用当前用户 `AppData` 目录继承的用户 ACL，不把 POSIX mode 位伪装成 Windows 权限检查。
- 车辆 SN、车主昵称/手机号和九号上游行程 ID 只存在于 Electron 主进程，不进入页面契约；页面只接收临时车辆 ID、自有/共享关系、经校验的激活/共享授权时间、智能服务剩余天数与脱敏后的只读状态。自有车辆只展示激活日期，共享车辆只展示当前账号获得共享权限的日期；实时位置仅在单独授权后进入位置页面。
- `whoami` 返回的手机号、邮箱、用户名、UUID、头像与第三方 OpenID 原文不会进入页面；主进程仅生成带星号的账号标识，并传递是否设置密码这一布尔状态。
- 退出账号会删除 `tokens.json` 和 `vehicles.json`，保留非敏感 `config.json`。
- 客户端会在 Electron 主进程内存中短时缓存解析后的设备快照、月度汇总、每日里程、可选行程列表和行程详情，其中行程详情可能包含 GPS 轨迹。该缓存不写入磁盘、不保存 ninecli 原始响应；手动刷新会绕过缓存，退出登录、成功刷新车辆列表或退出应用后失效。
- 用户主动导出的 GPX、CSV 或 JSON 文件保存在系统保存窗口中选择的位置。应用不会自动同步、上传或再次扫描这些文件。

## 网络请求

客户端可能产生以下网络请求：

1. ninecli 向九号相关服务发送登录或只读查询，用于账号、车辆状态、电池和行程。车辆状态的原始响应可能同时包含位置字段。
2. `uvx` 首次准备或缓存失效时，从 Python 包源获取固定的 `ninecli==0.1.7`。应用在执行其二进制前按 macOS Apple Silicon 或 Windows x64 平台分别核对固定 SHA-256。
3. 轨迹地图向 OpenFreeMap 请求当前轨迹区域的样式、瓦片、字体和图标。
4. 车辆位置地图只有在用户点击“读取并显示车辆位置”后才会接收坐标并请求位置区域瓦片；进入地图页本身不会把坐标交给页面或创建地图。

OpenFreeMap 能从瓦片请求推断正在查看的大致区域。客户端不会把九号账号、密码、令牌、车辆 SN、行程 ID 或完整轨迹 JSON 放入地图请求参数。

## 行程导出

- 导出操作必须从当前行程页面主动触发，并在系统保存窗口中确认路径；页面不能自行指定或获知完整目录路径。
- 三种导出格式都包含精确经纬度、时间、逐点速度、本次能耗、耗电百分比和骑行日里程。GPX 适合轨迹软件，CSV 适合表格分析，JSON 适合完整备份、编程与 AI 分析。
- 导出文件离开应用的本地会话边界后由用户自行保管。向他人或在线 AI 服务上传前，应确认愿意披露骑行位置和时间。

## 年度摘要导出

- 年度摘要只能在年度统计加载完成后主动触发，直接使用当前会话已读取的月度汇总和每日里程，不会额外请求 GPS 行程详情。
- CSV 和 JSON 年度摘要包含车辆显示名称、年份、历史数据起点、年/月次数/里程/时长/能耗、平均单次里程、平均能耗、每日里程，以及汇总完整性和明细截断标记；不包含 GPS 坐标、轨迹点、车辆 SN 或九号内部行程 ID。
- 即使没有精确位置，每日出行日期、频率和里程仍可能暴露个人活动规律。向在线 AI 或他人分享前应检查文件内容。

## 月度清单导出

- 月度清单只能从当前月份主动触发，并在系统保存窗口中确认路径；它直接复用已经加载的月度汇总、每日里程、可选行程和当前会话中的极速校验结果，不会为导出额外读取 GPS。
- CSV 和 JSON 同时包含完整月总次数、里程、时长、能耗与每日里程，以及 ninecli 当前返回的最多 20 条可选行程。逐次记录包含时间、里程、时长、能耗、电量消耗、骑行日累计、平均速度和极速依据，但不包含 GPS 坐标、轨迹点、车辆 SN、临时车辆 ID 或九号内部行程 ID。
- 未经轨迹校验的 `25 km/h` 旧摘要只作为接口原值保留，最终显示极速为空并标记为 `unverified-capped`；文件仍能暴露个人出行时间和活动规律，分享前应检查内容。

## 登录凭据与位置

- 受 ninecli CLI 限制，密码或短信验证码在登录瞬间会作为本机子进程参数传递。同一台电脑上具有相同权限的进程工具可能在该瞬间观察到参数。客户端不记录这些参数。
- 短信登录分为发送验证码和提交验证码两步。若九号要求网易易盾人机验证，客户端不会规避该检查，只提示用户改用密码登录。
- `status` 原始响应可能包含 `loc`。普通设备快照在 Electron 主进程解析时会丢弃坐标，不把原始响应或经纬度发送给页面。车辆位置显示需要每个会话、每辆车单独授权；授权后独立桥接会重新查询状态，位置页面只接收共享位置权限，以及允许情况下的坐标、锁车和 ACC 状态。若 `permissions.see_location` 明确拒绝访问，即使异常响应夹带坐标，主进程也会丢弃坐标，页面不会创建地图或请求位置区域瓦片。停止显示、切换车辆或退出登录会清除页面中的坐标。
- 坐标校准预览使用应用内固定版本的本地转换库，只生成地图覆盖物副本。它不把坐标提交给转换服务，也不修改原始轨迹、统计或导出；切换后地图视野可能变化，OpenFreeMap 会收到新视野对应的瓦片请求。

## 产品边界

- 客户端不开放鸣笛、解锁、启动、熄火、开座桶等车辆控制。
- ninecli 与九号云端接口不是本项目提供的官方 API，可能变化或停止工作。
- 当前应用不代表九号公司，也不声称得到品牌或商业授权。
