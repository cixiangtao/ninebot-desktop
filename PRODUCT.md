# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Electron + React + TypeScript + Vite，使用 pnpm 管理依赖；Tailwind CSS 负责样式系统，Oxlint 与 Oxfmt 负责静态检查和格式化。技术栈由用户确认。

## Users

第一阶段服务产品作者本人，在 macOS 或 Windows 桌面端直观查看自己的九号车辆和骑行记录；后续面向其他九号车主打包分享与发布。

## Product Purpose

把九号云端已返回但官方电自轨迹详情页未充分展示的数据做成直观的桌面体验，包括车辆、骑行记录、轨迹、速度曲线、极速、单次能耗和电量消耗。成功意味着用户无需抓包或修改 IPA，也能在本机完成登录、查询和可视化。

## Positioning

产品通过只读本地客户端调用九号云端数据，并直接解析电自行程返回的 `speed` 与分号分隔 `trail`，补足官方 App 对电自极速数据的展示缺口。

## Operating Context

用户在个人电脑上使用九号账号登录，区分自有与共享车辆，分别查看主电源与 ACC，逐组查看单/双电池的类型、电量、续航、锁车状态、BMS 指标和循环说明，浏览月度行程，并选择单次骑行查看地图轨迹、速度曲线、极速、里程、时长、计算平均速度、本次能耗和电量消耗。账号令牌、车辆 SN、车主身份、实时位置和 GPS 轨迹均属于本机敏感数据。

## Capabilities and Constraints

- 第一版继续以 `ninecli==0.1.7` 作为核心运行时，不立即重写 Passport 签名、业务登录和数据加解密；纯 Node 协议实现延后评估。
- ninecli 必须经由受控适配层执行：固定版本、固定二进制哈希、最小环境变量和明确命令白名单；校验失败或平台未建立基线时不得执行业务命令。
- 当前开放登录、账号校验、车辆列表、脱敏后的 `status` / `battery` 只读状态、行程列表和行程详情，不开放鸣笛、启动、熄火或开座桶等车辆控制。
- `status` 原始响应可能带有 `loc`；普通设备快照在主进程解析时丢弃坐标。进入地图页不等于授权，只有用户明确确认后，独立位置桥接才重新查询状态、把坐标交给渲染层并加载第三方瓦片。
- 轨迹历史按年份和月份浏览；客户端按选中月份向九号接口请求数据，不一次性抓取多年 GPS 行程。月度总量和每日里程使用 `travel` 顶层完整汇总，可选择明细的 20 条上限单独披露。
- 支持密码登录；短信登录可能触发网易易盾人机验证，当前 `ninecli` 无法完成该交互。
- 九号云端接口属于非官方、未文档化接口，可能随服务端更新失效。
- `status` 与 `battery` 作为独立数据域并发读取；单个普通上游错误允许返回另一域的成功结果，但认证、验证、运行时和完整性错误必须整体失败。
- 设备状态自动监看只能由用户显式开启，默认每 60 秒读取一次，并在设备页不可见、窗口隐藏或已有读取进行中时暂停；它不构成后台常驻服务，也不保存变化历史。
- 已完成 macOS Apple Silicon 与 Windows x64 打包、ASAR/Fuses 安全加固、隐私说明、崩溃恢复、产物校验和成品/安装后启动冒烟；稳定公开发布仍需平台签名、公证/信誉、自动更新及 ninecli 再分发证据闭环。
- 运行时安全状态、当前令牌状态和“退出并清除本地令牌”必须在设置页可见、可操作。
- UI 与平台能力通过稳定、版本化、面向领域的桥接契约隔离，为未来 PWA、Capacitor App 或小程序适配保留空间。
- 桌面端使用 MapLibre GL JS 加载 OpenFreeMap 在线底图，轨迹覆盖物保持本地渲染，并在底图超时或不可用时回退到本地轨迹画布。
- 在线底图请求会向第三方暴露当前视野的大致位置区域；界面和发布文档必须持续明确这一边界，不得把“本地优先”表述成“完全离线”。

## Evidence on Hand

- 已验证九号账号可通过 `ninecli` 密码登录。
- 已验证车辆列表和月度骑行记录可读取。
- 已验证 `status.dump_energy` 与电池诊断接口的当前电量一致；它可在独立的 `battery` 服务临时失败时保留基础电量，并且客户端会明确标注读数来源。两个接口同时可用但相差至少 5 个百分点时，页面并列展示两者而不是静默掩盖。
- 已验证 `vehicles` 可区分自有/共享关系并返回智能服务剩余天数；`status` 与 `battery` 可读取电量、续航、锁车、主电源、ACC、锂电类型、充电完成时间，以及逐组电池温度、电压、循环次数、评分与 BMS 说明；SN、车主身份、实时经纬度和外部图片 URL 不进入普通状态契约。
- 已验证真实 `vehicles` 响应使用 `actived` 表示激活状态，`active_date` 与 `auth_date` 为 Unix 秒且授权时间不早于激活时间；客户端按自有/共享关系分别显示激活日期或当前账号授权日期，并丢弃状态矛盾、越界和倒序时间。
- 已验证真实响应中 `pwr=true`、`loc.acc=false` 可同时存在，因此两者不是同一状态；客户端使用顶层 `pwr` 表示主电源，只在位置桥接中把 `loc.acc` 表示为 ACC。含义不明的座桶锁和内部续航选择枚举保持排除。
- 已验证真实 `battery` 响应可能在 `battery_count=0` 时仍返回一组有效电池，并在 BMS 支持标记为 `false` 时仍返回有效循环次数和说明；这两个矛盾字段保持排除，页面只按实际电池数组与有效诊断值展示。
- 已通过故障注入验证 `status` 单独失败时仍保留电池/BMS 数据，`battery` 单独失败时仍保留锁车、主电源、ACC 与续航；两域同时失败以及认证错误保持失败，不进行不安全降级。
- 已验证位置授权前坐标不会进入渲染层、没有车辆地图实例或新增瓦片请求，授权后才加载演示位置，停止显示后立即卸载；真实账户自动化停留在授权页，不代替用户向第三方披露实际车辆区域。普通设备状态查询在主进程收到的原始响应可能包含 `loc`，解析器会主动丢弃坐标。
- ninecli 的共享车辆状态可通过 `permissions.see_location` 明确表达位置权限。只有用户主动授权后，独立位置桥接才提炼 `allowed` / `denied` / `unknown`；显式拒绝优先于异常夹带的坐标，页面不创建地图或请求瓦片，并提供调整共享权限后的重新检查入口。
- 已验证单次电自行程顶层直接返回 `speed` 极速字段。
- 已验证 `trail` 为 `经度,纬度,速度,附加值;...` 格式；实测样本包含 212 个速度点，轨迹最高速度 70 km/h，接口声明极速 71 km/h。
- MapLibre 的 `LngLat` 契约明确使用 WGS84（EPSG:4326），但 ninecli 的轨迹和位置输出没有坐标制元数据。固定版本的本地 `gcoord` 只用于生成 GCJ-02→WGS84 对照副本，并显示约数位移；接口原值仍是默认和唯一导出源。
- 已验证 `travel` 的 `ec` 和 `used_electricity` 分别对应单次能耗（Wh）和电量消耗（%）；真实历史记录可读取、显示、对比并随 GPX、CSV、JSON 导出，缺失值保持为空而不是按零处理。
- 单次平均能耗严格使用有效 `ec ÷ 正里程` 计算，在详情与同月对比中以 Wh/km 展示；缺失能耗和零里程保持不可用，差值不附加好坏判断。
- 已验证满 20 条的月份中，`travel` 顶层 `times`、`total_mileages`、`duration`、`ec` 均大于可见明细求和；未触顶月份则完全相等。顶层字段可作为完整月度汇总，明细列表只能作为可选择样本。
- 已验证 `travel` 顶层 `detail` 数组按自然日提供完整每日里程；实账号当月 31 个条目的求和与 `total_mileages` 一致，可用于月度骑行日历、活跃天数和单日最高里程，而无需加载 GPS 详情。
- 已验证真实账号当前 20 条可选行程的 `day_total_mileage` 均为有效数字，且逐条匹配顶层完整每日里程；客户端可据此展示骑行日累计和本次占比，矛盾值保持不可用。
- 已验证 `travel` 顶层 `first_time` 在活跃月、空月和历史起点前月份保持一致，可作为车辆历史数据起点；月份选择器和年度统计请求可据此裁剪，不需要逐月试探更早的空记录。
- 年度统计可将已加载的年度总量与平均值、月度汇总与平均值、每日里程和覆盖标记导出为 CSV/JSON schema v2，不额外读取 GPS 详情，也不输出车辆 SN 或九号内部行程 ID；活跃月份缺失能耗或日统计时相应年度值保持为空。
- 已加载的完整每日里程可在本地形成年度活动热力图，并可靠计算最高里程日、最长连续骑行和最常骑行星期；历史起点前、未来和未知日期不会按零里程参与计算。
- 已验证当前车辆历史从 2025 年 11 月开始；当前 2026 年 8 月的“近 12 月”窗口可连续合并 2025 年 11 月至 2026 年 8 月的 10 个真实月份，窗口中的 2025 年 9 月和 10 月明确禁用，不将起点前数据伪装为零。
- 已验证历史月份的 20 条可选行程可按 3 路并发读取详情并逐条获得轨迹采样极速；批量结果只包含匿名行程 ID、接口极速和采样极速，不包含 `trail`、经纬度、车辆 ID 或九号内部行程 ID，可用于在月度列表中纠正固定 25 km/h 的旧摘要。
- 已用真实账号验证月度清单导出：完整月总量和每日里程与 `travel --month` 保持一致，20 条可选行程全部携带轨迹极速依据；输出不包含 GPS、车辆 SN、临时车辆 ID 或九号内部行程 ID，未校验的旧 `25 km/h` 摘要不会进入最终显示极速。
- 已用真实账号验证设备页自动监看：开启后立即得到一份 location-free 对照快照，能显示具体变化或“状态未发生变化”，关闭后轮询停止且最后事件仍可核对；1440×900 与 1080×680 均无横向溢出。
- 没有可用于公开发布的九号官方 API 授权、品牌授权或商业背书，不得虚构。

## Product Principles

- 只读优先：默认能力只观察数据，不改变车辆状态。
- 本地优先：凭据、令牌、SN 和 GPS 数据不上传到自建服务。
- 数据诚实：区分接口声明极速、轨迹采样最高值和计算平均速度。
- 渐进替换：先交付可靠的桌面体验，再逐步替换非官方底层依赖。
- 发布可持续：从第一版就保持清晰的进程边界、类型契约和可测试性。
- 按需加载：历史记录以月为查询边界，快速切换时只接受最后一次选择的结果。

## Capability Roadmap

1. 已完成多车辆切换：完整呈现 `vehicles` 返回的拥有与共享车辆，设备状态、行程和请求竞态均绑定当前临时车辆 ID。
2. 已完成月度与年度统计：使用 `travel` 顶层完整汇总计算次数、里程、时长、能耗和平均能耗；单月可选明细达到 20 条上限时显示“明细 20/总次数”，但不再低估年度总量，并且始终避免请求 GPS 详情。
3. 已完成按需车辆位置页：普通设备快照在主进程丢弃 `status.loc` 坐标，授权前零坐标披露/瓦片请求；授权后独立桥接重新读取位置，切换车辆或停止显示会清除页面坐标。地图默认保持接口原值，并提供不改写原始数据的 `GCJ-02 → WGS84` 本地校准预览，用实车道路贴合情况继续确认坐标基准。
4. 发布基础设施进行中：已完成 Apple Silicon DMG/ZIP、Windows x64 NSIS/ZIP、原创图标、ASAR 完整性、安全 Fuses、受控 `ninebot-desktop://app` 协议、隐私说明、崩溃恢复、统一 SHA-256 清单和成品冒烟；待完成 Developer ID/公证、Windows Authenticode、ninecli 再分发证据与自动更新。
5. 已完成同月行程对比：按需读取第二条 `travel --detail`，比较轨迹实测极速、平均速度、里程、用时和五档速度里程占比；差值保持中性，不把不同路线与路况简单排名。
6. 已完成行程能耗覆盖：读取 `ec` 与 `used_electricity`，在列表、详情和同月对比中展示 Wh 与电量消耗百分比，并完整保留到 GPX、CSV、JSON 导出。
7. 已完成车辆诊断语义修正：分离 `pwr` 主电源与 `loc.acc` ACC，接入锂电类型、BMS 循环说明、智能服务状态及可用的充电完成时间，并对未知枚举保持空值而不是猜测。
8. 已完成设备遥测分区降级：`status` / `battery` 任一普通上游故障时保留另一域并标出缺失项；双域失败与安全类错误继续阻断，避免单接口波动清空整页或掩盖登录问题。
9. 已完成会话读取缓存：设备快照、月度列表和行程详情按短时 TTL 复用解析后的领域对象，相同并发请求合并；缓存仅在主进程内存中存在，不保存原始响应，手动刷新会绕过缓存。
10. 已完成月度骑行日历：解析 `travel` 顶层完整每日里程，展示活跃天数和单日最高里程；点击日期可筛选当天可选行程，若明细被 20 条上限截断则保留里程并明确提示。
11. 已完成历史数据边界：解析 `first_time`，在月份选择器显示数据起点并禁用更早月份；年度统计只请求起点至当前月，起点前月份明确标为“数据起点前”。
12. 已完成年度摘要导出：CSV 面向 Excel，JSON 面向备份、编程与 AI；两者都保留月/日汇总和截断标记，并排除 GPS、车辆 SN 与内部行程 ID。
13. 已完成年度活动洞察：将完整每日里程呈现为全年热力图，计算最高里程日、最长连续骑行和最常骑行星期；跳转只读取对应月份列表，用户继续选择具体行程后才加载 GPS。
14. 已完成多指标月度趋势：在已经读取的 `travel --month` 汇总上切换里程、次数、平均单次里程、能耗、活跃天数和平均能耗，不为切换指标追加 ninecli 请求；图表柱与月度明细都可进入对应月份列表，仍需用户选择具体行程后才读取 GPS。
15. 已完成当前行程强制刷新：详情操作区可绕过 10 分钟内存缓存重新执行 `travel --detail`，保持当前行程和相对回放进度；刷新失败保留上次成功数据并提供明确重试状态，演示模式不伪造刷新结果。
16. 已完成跨接口电量回退：优先采用独立 `battery` 诊断读数；该域临时失败时回退到 `status.dump_energy`，保留基础电量、续航与车辆状态。设备页持续标出读数来源，并在两个接口相差至少 5 个百分点时并列披露。
17. 已完成共享位置权限诊断：显式解析 `status.permissions.see_location`，区分共享权限拒绝、权限未知、允许但暂无坐标和正常位置；拒绝时剥离坐标、保持零瓦片请求，并指导用户让车主调整权限后重新检查。
18. 已完成车辆生命周期资料：解析 `actived`、`active_date` 与 `auth_date`，自有车辆展示激活日期，共享车辆展示获得共享权限的日期；显式未激活与异常时间不会被包装成可信资料。
19. 已完成单次平均能耗：使用已验证的 `ec` 和行程里程计算 Wh/km，在轨迹详情与同月行程对比中统一展示；缺失能耗或零里程不进行推断，并明确排除语义矛盾的电池计数/BMS 支持标记。
20. 已完成年度导出 schema v2：CSV 增加年度汇总行，CSV/JSON 都直接提供年/月平均单次里程和平均能耗，以及完整性与截断标记；复用已加载摘要，不新增 ninecli 或 GPS 请求。
21. 已完成平均单次里程趋势：使用完整月度总里程除以完整月度次数，与年度导出 schema v2 采用同一口径；切换图表不新增请求，零次月份保持为 0，且不受 20 条可选行程上限影响。
22. 已完成骑行日背景：解析并验证 `day_total_mileage`，在行程详情展示当天累计里程与本次占比；缺失或小于本次里程的矛盾值不进入计算，单次 GPX/CSV/JSON 导出同步保留，JSON 升级为 schema v2。
23. 已完成近 12 个月连续趋势：当前年份加载时将自然年与截至本月的滚动 12 个月请求去重合并，并按 `first_time` 裁掉历史起点前查询；切换范围不新增 ninecli/GPS 请求，起点前月份禁用，年度导出仍严格保持自然年口径。
24. 已完成本月极速校验：未经轨迹验证的 25 km/h 旧摘要只显示“待校验”，不作为真实极速展示；用户显式触发后，以最多 3 个并发请求校验 ninecli 当前暴露的最多 20 条行程详情，在侧栏逐条显示轨迹采样极速并标出被纠正的旧摘要。渲染层只接收速度摘要，普通上游失败允许保留已成功结果，安全类错误继续整体阻断。
25. 已完成设备状态自动监看：默认关闭，开启后立即读取并每 60 秒刷新 location-free 的 `status` / `battery` 摘要，展示电量和核心开关状态变化；离页与窗口隐藏会暂停，切车与退出会关闭监看并清空记录，变化记录只存在于当前渲染会话。
26. 已完成坐标校准预览：MapLibre 明确按 WGS84 接收经纬度，而 ninecli 未声明轨迹与位置坐标制；在线地图默认保持接口原值，中国大陆范围内可在会话中显式切换本地 `GCJ-02 → WGS84` 覆盖物副本并查看大约位移。该模式不修改原始轨迹、速度、里程、统计或导出，也不调用外部转换服务。
27. 已完成月度逐次清单导出：CSV/JSON 同时保留完整月汇总、每日里程和当前最多 20 条可选行程，明确标注截断范围；逐条提供能耗、骑行日背景、接口极速、轨迹极速与依据类型，未校验的旧 `25 km/h` 最终值保持为空，并排除 GPS、车辆 SN 与所有内部标识。

`login-code` 在 ninecli 能完成网易易盾验证交互之前保持暂缓；鸣笛、开座桶、启动和熄火继续永久排除在只读产品之外。

## Visual Direction

- 用户已选择「轨迹回放优先」作为第一版的正式构图。
- 视觉语言采用九号 App 的清爽浅色数据卡片与 iOS/macOS 原生层级：淡灰背景、白色大圆角卡片、深蓝黑文字、青绿色路线和克制的半透明材质。
- 首屏以全幅浅色地图为主体，左侧为车辆与行程导航，右上浮动极速指标卡，底部为速度曲线与播放时间轴。
- 批准稿：`.impeccable/mocks/composition/replay-first.webp`。

## Accessibility & Inclusion

桌面客户端需要完整键盘导航、清晰焦点状态、足够文字与图表对比度，并避免仅靠颜色表达速度或错误状态。
